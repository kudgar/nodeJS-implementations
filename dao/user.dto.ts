// user.dto.ts

/**
 * 2. Определите DTO (Data Transfer Object). DTO — это объект, который переносит данные между процессами.
 */
export interface UserDTO {
  id: number;
  username: string;
  email: string;
}
